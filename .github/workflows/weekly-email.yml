name: Weekly Email

on:
  schedule:
    # Email weekly cron - runs every Monday at 13:00 UTC (matches Vercel schedule: "0 13 * * *")
    - cron: '0 13 * * 1'
  # Allow manual triggering for testing
  workflow_dispatch:

jobs:
  email-weekly:
    name: Email Weekly Digest
    runs-on: ubuntu-latest
    steps:
      - name: Call Email Weekly Endpoint
        env:
          VERCEL_URL: ${{ vars.VERCEL_URL }}
          ADMIN_API_KEY: ${{ secrets.ADMIN_API_KEY }}
        run: |
          if [ -z "$VERCEL_URL" ]; then
            echo "Error: VERCEL_URL variable is not set"
            exit 1
          fi
          if [ -z "$ADMIN_API_KEY" ]; then
            echo "Error: ADMIN_API_KEY secret is not set"
            exit 1
          fi
          
          echo "Calling email weekly endpoint..."
          response=$(curl -s -w "\n%{http_code}" -X GET \
            -H "x-admin-api-key: $ADMIN_API_KEY" \
            "$VERCEL_URL/api/cron/email-weekly")
          
          http_code=$(echo "$response" | tail -n1)
          body=$(echo "$response" | sed '$d')
          
          echo "Response HTTP Status: $http_code"
          echo "Response Body: $body"
          
          if [ "$http_code" -ge 200 ] && [ "$http_code" -lt 300 ]; then
            echo "✓ Email weekly cron completed successfully"
          else
            echo "✗ Email weekly cron failed with HTTP $http_code"
            exit 1
          fi
