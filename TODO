- [x] set up Resend with our domain for sending
- [x] create a page like the health check page that lists all events it would email about
- [x] make a way to send myself test emails
- [x] vercel login and figure out deployment
- [x] set up the database
- [x] yarn migrate successfully
- [x] have test email include events. make them be fairly plain -- just text and links. it must look like a person made it, not a stylish html template

## Phase 1: Email Control & Discord Logging Infrastructure

- [x] Add EMAIL_ENABLED environment variable to `.env.example` (defaults to false)
- [x] Add EMAIL_ENABLED to `src/lib/env.ts` with default false
- [x] Create `sendDiscordEmailLog()` helper function in `src/lib/discord.ts` for logging email operations
- [x] Create `sendDiscordInfo()` helper function in `src/lib/discord.ts` for general success/info logging (not just errors)
- [x] Update all email functions in `src/lib/email.ts` to check EMAIL_ENABLED before sending
- [x] Add Discord logging to all email functions (log recipient count, never email addresses)
- [x] If EMAIL_ENABLED is false: log "Would send to X recipients (emailing disabled)"
- [x] If EMAIL_ENABLED is true: log "Sent to X recipients"

## Phase 2: API Readiness for External Site (fractal.boston)

- [x] Add CORS headers to `/api/verify` route to allow requests from fractal.boston
- [x] Add CORS headers to `/api/unsubscribe` route to allow requests from fractal.boston
- [x] Add CORS headers to `/api/subscribe` route to allow requests from fractal.boston
- [x] Verify API endpoints use POST only (no GET support needed)
- [x] Verify APP_URL environment variable is configured correctly (should be https://fractal.boston in production)
- [x] Verify all email links point to fractal.boston (verify/unsubscribe pages are in separate project)
- [x] Create API.md documentation file with:
  - All endpoint documentation (subscribe, verify, unsubscribe)
  - Request/response formats
  - Example client-side code (fetch/axios) for fractal.boston
  - Error handling documentation
  - Required headers (X-Api-Key for subscribe)

## Phase 3: Comprehensive Discord Logging

- [x] Add success logging to `/api/subscribe` route (log successful subscriptions)
- [x] Add success logging to `/api/verify` route (log successful verifications)
- [x] Add success logging to `/api/unsubscribe` route (log successful unsubscribes)
- [x] Add logging to `/api/webhooks/luma/subscriber` route (log successful subscriber additions)
- [x] Add logging to `/api/webhooks/luma/event` route (log successful event creations)
- [x] Update `/api/verify/route.ts` to log email operations (welcome email sending)
- [x] Update `/api/webhooks/luma/subscriber/route.ts` to check EMAIL_ENABLED and log email operations
- [x] Update `/api/webhooks/luma/event/route.ts` to check EMAIL_ENABLED and log email operations
- [x] Update `/api/cron/weekly/route.ts` to uncomment email sending code, add EMAIL_ENABLED check, and add Discord logging

## Phase 4: Subscriber Import Scripts

- [x] Research Luma API endpoints for fetching calendar subscribers
- [x] Create `scripts/pull-luma-subscribers.ts` script
- [x] Import Luma subscribers with source="luma" and status="verified"
- [x] Handle duplicate emails (check by email before inserting)
- [x] Add Discord logging for Luma import stats (new subscribers added, duplicates skipped)
- [x] Research Substack API endpoints for fetching subscriber list
- [x] Add SUBSTACK_API_KEY to `.env.example` with documentation
- [x] Create `scripts/pull-substack-subscribers.ts` script
- [x] Import Substack subscribers with source="substack" and status="verified"
- [x] Handle duplicate emails (check by email before inserting)
- [x] Add Discord logging for Substack import stats (new subscribers added, duplicates skipped)

## Phase 5: Verification & Testing

- [ ] Verify email footer in `src/lib/email.ts` includes all Fractal Boston links (fractal.boston, Calendar, Discord, Unsubscribe)
- [ ] Verify footer appears in all email types (verification, welcome, weekly digest, new event alert)
- [ ] Verify Discord and email use same events (both use `getReportableEvents()`)
- [ ] Test API endpoints from external domain (fractal.boston) - verify CORS works
- [ ] Test subscribe flow: form → verification email → verify page (on fractal.boston) → welcome email
- [ ] Test unsubscribe flow: email link → unsubscribe page (on fractal.boston) → confirmation
- [ ] Test subscriber import scripts (both Luma and Substack)
- [ ] Test that all Discord logging works correctly
- [ ] Verify email links point to correct external URLs (fractal.boston/verify?token=xxx, fractal.boston/unsubscribe?token=xxx)
